include: package:flutter_lints/flutter.yaml

linter:
  rules:
    prefer_const_constructors: true
    prefer_const_literals_to_create_immutables: true
    avoid_print: false # debugPrint is used intentionally for gated diagnostic logging

analyzer:
  exclude:
    - build/**
    - "**/*.g.dart"
    - android/**
